# 개발·재개 안내 · v2.0.0

현재 상태는 제작진행.md와 검증결과.md를 먼저 확인한다. 차기업데이트-요구사항.md는 최초 요구 기록이다. 기존 코드에서 이어서 고치며 새 프로젝트를 만들지 않는다.

## 구성

- src/main.js: 로그인, 화면, 직접 조작, 작업, 자동 저장, 긴급 사건.
- src/world.js / regions.js: 연결된 이동 공간과 충돌, NPC, 8개 장면, 지도, 페로몬, 굴착과 그림.
- src/progression.js / content.js: 기존30개 임무 및 등급. 기존 임무 식별자/순서를 보존한다.
- src/campaign.js: 새 이야기23단계, 챕터, 동맹, 퍼즐, 출입 조건, 도구 및 저장 정규화.
- src/adventure.js: 실제 대상과 상호작용하는 감옥·탈옥·여왕전·원정 제어.
- src/dialogue.js / audio.js: 한 글자 출력과 타이머 해제, 장면별 음악과 음량 버스.
- src/storage.js / apps-script/Code.gs: 계정, 충돌 없는 저장, 한글 왕복 자료, PIN 문자열.

## 저장 계약

state.version=1은 기존 임무 자료 계약이며 게임 배포 버전과 별개다. 게임 VERSION/package/index/서버 버전은2.0.0이다. API로그인응답의 serverVersion이2 미만이면 클라이언트에서 새 이야기 손실 전에 막는다.

state.campaign.단계0은 기본 임무, 1~23은 새 이야기, 24는 여왕전 완료다. 기존 여왕은 단계24로 복원한다. 기존 마지막 royal임무는 단계24에서만 완료된다. 이야기 종료와 즉위 저장 사이에 중단된 경우 로그인 때 완료 처리를 이어간다.

캠페인의 한글 키/값을 Apps Script와 함께 변경해야 한다. 왕관·지역·감옥·수색·도구·동맹·긴급 상태를 저장하며 내부장면ID는 시트에서 한글명으로 변환한다. 31개 열 유지, 기존 열/플레이어 행 삭제 금지. PIN은 텍스트 형식으로 쓰며 숫자로 저장됐던 과거PIN도 네 자리 복원한다.

일반 감옥은45초·씨앗5개·청소로 석방, 이야기 감옥은 균열3회 후 통로 개방. 자동굴착은160초마다 한 단계, 최대7에서 정지. 현장8번째 작업이 개통이고 이후 일거리는 보강이다. 이전 저장에서 이미 개통된 길은 유지한다.

## 검사와 배포

Node.js24 기준 npm test / npm run check. 로컬 브라우저 검사는 ../../work/qa-v2.mjs와 후속 검사 스크립트를 사용한다. 이 테스트는 별도 모의 Google응답으로 사용자 계정을 쓰지 않는다. 이동/일부 대기시간을 단축해 흐름을 검사하므로 실제 첫 클리어 소요시간 실측이 아니다.

배포 방식은 사용자가 Apps Script를 기존 웹앱의 새 버전으로 배포하고 전체ZIP내용을 GitHub루트에 업로드하는 방식이다. 노드모듈·별도빌드필수로 바꾸지 않는다. 버전별ZIP은 이전 것을 보존하고 새이름으로 만든다. 현재 버전은src/config.js와package.json/index.html/Code.gs에서 일치시킨다.

## 이야기 참고 방향

어썸피스의 엘프고·마법고·은하고에서 챕터별 지역 개방, 허브 탐험, 동료의 부탁과 연합 형성이라는 구성 방향을 참고했다. 인물·대사·군락 갈등은 이 게임용으로 새로 작성했다.
참고: https://recruit.awesomepiece.com/ · https://www.namu.moe/w/좀비고등학교/엘프고등학교/세계관과%20스토리 · https://m.namu.moe/w/좀비고등학교/은하고등학교/퀘스트
